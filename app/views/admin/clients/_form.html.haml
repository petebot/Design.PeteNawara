= render "common/form_errors", :target => @client
- form_for @client do |form|
  = form.label :name
  = form.text_field :name
  = form.label :url
  = form.text_field :url
  %button{:type => "submit"}= params[:action].capitalize
  = link_to "Cancel", admin_clients_path